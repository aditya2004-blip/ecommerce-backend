# Use an H2 in-memory database or a test-specific DB
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db
spring.datasource.username=root
spring.datasource.password=Ayush$2004

# Dummy secrets for testing (No need to hide these as they are for tests only)
jwt.secret=testing_secret_key_that_is_long_enough_for_hs512_verification_2026
jwt.expiration=3600000

# Mock/Local settings for Redis and Mail
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.mail.host=localhost